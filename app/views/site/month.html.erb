<% if @events_by_category.empty? %>
  <p style="color: #999; padding: 3rem 0; text-align: center;">No events listed for <%= @month_name %> yet.</p>
<% else %>
  <%
    other_events     = @events_by_category["other"]
    community_events = @events_by_category["community"]
    main_categories  = @events_by_category.reject { |cat, _| %w[other community].include?(cat) }
  %>
  <div class="events-grid">
    <% main_categories.each do |category, events| %>
      <section class="category-section" data-category="<%= category %>">
        <div class="category-header">
          <h2><%= category %></h2>
          <span class="category-count"><%= events.length %></span>
          <button class="category-toggle" aria-label="Toggle <%= category %>">−</button>
        </div>
        <div class="event-list">
          <% events.each do |event| %>
            <div class="event-card fade-in">
              <div class="event-card-inner">
                <% if event[:image_url].present? %>
                  <img src="<%= event[:image_url] %>" alt="" class="event-card-image" loading="lazy">
                <% end %>
                <div class="event-card-content">
                  <div class="event-name">
                    <% if event[:url].present? %>
                      <a href="<%= event[:url] %>" target="_blank" rel="noopener"><%= event[:name] %></a>
                    <% else %>
                      <%= event[:name] %>
                    <% end %>
                  </div>
                  <div class="event-meta">
                    <% if event[:date_text].present? %><span class="date"><%= event[:date_text] %></span><% end %>
                    <% if event[:venue].present? %><span class="venue"><%= event[:venue] %></span><% end %>
                  </div>
                  <% if event[:description].present? %>
                    <p class="event-description"><%= event[:description].truncate(160) %></p>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </section>
    <% end %>
  </div>

  <% if community_events.present? %>
    <section class="category-section" data-category="community">
      <div class="category-header">
        <h2>community</h2>
        <span class="category-count"><%= community_events.length %></span>
        <button class="category-toggle" aria-label="Toggle community">−</button>
      </div>
      <div class="event-list">
        <% community_events.each do |event| %>
          <div class="event-card fade-in">
            <div class="event-card-inner">
              <div class="event-card-content">
                <div class="event-name">
                  <% if event[:url].present? %>
                    <a href="<%= event[:url] %>" target="_blank" rel="noopener"><%= event[:name] %></a>
                  <% else %>
                    <%= event[:name] %>
                  <% end %>
                </div>
                <div class="event-meta">
                  <% if event[:date_text].present? %><span class="date"><%= event[:date_text] %></span><% end %>
                  <% if event[:venue].present? %><span class="venue"><%= event[:venue] %></span><% end %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </section>
  <% end %>

  <% if other_events.present? %>
    <section class="category-section" data-category="other">
      <div class="category-header">
        <h2>other</h2>
        <span class="category-count"><%= other_events.length %></span>
        <button class="category-toggle" aria-label="Toggle other">−</button>
      </div>
      <div class="event-list">
        <% other_events.each do |event| %>
          <div class="event-card fade-in">
            <div class="event-card-inner">
              <div class="event-card-content">
                <div class="event-name">
                  <% if event[:url].present? %>
                    <a href="<%= event[:url] %>" target="_blank" rel="noopener"><%= event[:name] %></a>
                  <% else %>
                    <%= event[:name] %>
                  <% end %>
                </div>
                <div class="event-meta">
                  <% if event[:date_text].present? %><span class="date"><%= event[:date_text] %></span><% end %>
                  <% if event[:venue].present? %><span class="venue"><%= event[:venue] %></span><% end %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </section>
  <% end %>
<% end %>
